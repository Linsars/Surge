# 🚀 Cloudflare Worker 快速生成器

这是一个极简的工具，旨在帮助你一键获取基础的 Cloudflare Worker 模板。无需配置复杂的开发环境，点击下方按钮即可开始。

## 🛠️ 在线一键生成

你可以通过以下按钮直接访问托管在 GitHub Pages 上的生成器：

[![使用 Cloudflare Worker 生成器](https://img.shields.io/badge/Run_Generator-Click_Here-6366f1?style=for-the-badge&logo=cloudflare)](https://raw.githubusercontent.com/Linsars/Surge/main/index.html)

> **注意：** 请将上方链接中的 `你的用户名` 和 `仓库名` 替换为你实际的 GitHub 地址。

---

## 📝 部署说明

如果你想在自己的 GitHub 仓库中使用，请按照以下步骤操作：

1. **上传文件**：将 `index.html` 上传到你的仓库。
2. **开启 Pages**：
   - 进入仓库的 `Settings` -> `Pages`。
   - 在 `Build and deployment` 下，将 Source 设置为 `Deploy from a branch`。
   - 选择 `main` 分支并点击 `Save`。
3. **获取链接**：约 1 分钟后，你会得到一个类似 `https://xxx.github.io/xxx/` 的链接，将其填入上方 README 的链接处即可。

## 💡 生成的代码模板

生成的代码将采用最新的 **ES Modules** 标准：

javascript
export default {
};
